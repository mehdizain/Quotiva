import 'package:flutter/material.dart';

class AppTabStyles {
  static final background = Colors.white.withOpacity(0.15);
  static final activeBackground = Colors.white;
  static const activeTextColor = Colors.indigo;
  static const inactiveTextColor = Colors.white70;

  static const labelStyle = TextStyle(
    fontSize: 15,
    fontWeight: FontWeight.w600,
  );

  static const unselectedLabelStyle = TextStyle(
    fontSize: 15,
    fontWeight: FontWeight.w500,
  );
}
